services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: toppot
      POSTGRES_PASSWORD: toppot
      POSTGRES_DB: toppot
    ports:
      - "5432:5432"
    volumes:
      - toppot_pg:/var/lib/postgresql/data

volumes:
  toppot_pg:
